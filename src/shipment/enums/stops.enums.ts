export enum StopType {
	Pickup = "pickup",
	Delivery = "delivery",
}

export enum StopStatus {
	InTransit = "in-transit",
	Completed = "completed",
	Cancelled = "cancelled",
  Arrived = "arrived",
	Departed = "departed",
}
